// The party is a MODAL, not a tab page: it slides up from the bottom and the
// chevron sends it back down. The centre tab opens it (see TabBar).
export { default } from '@/party/LivePartyMockScreen';
