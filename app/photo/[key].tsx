/**
 * /photo/<key> — one diary photo. The key is the photo's clientId (stable even
 * while the upload is still pending); the screen lives in src/photos.
 */

export { default } from '@/photos/BeerPhotoDetailScreen';
