// Pushed route `/leaderboards` — the counter, the profile and Parta all link
// here with a `source` param. The Community tab renders the same screen with
// `embedded`, which is only the difference of a back chevron.
export { default } from '@/leaderboards/LeaderboardsScreen';
